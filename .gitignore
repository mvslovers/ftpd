*.s
*.o
*.d
*.jcl
!samplib/*.jcl
.build-warnings
compile_commands.json

# Connection settings, incl. MBT_MVS_PASS -- .env plus the per-target
# variants (.env.mvsdev, ...). The example carries no credentials.
.env
.env.*
!.env.example

# Generated build state -- the committed lock lives at the root (mbt.lock).
.mbt/
contrib/
dist/
build/
*.xmit

.cache/
.DS_Store
